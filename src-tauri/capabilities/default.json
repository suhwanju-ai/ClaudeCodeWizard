{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The frontend is deliberately granted no filesystem permission: every file access goes through a Rust command (IMP-026).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
